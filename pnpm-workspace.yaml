# Dependencias con scripts de build que pnpm ejecuta sin pedir confirmacion.
# Las tres son toolchain de build (compilador SWC, bundler, resolver de imports).
onlyBuiltDependencies:
  - '@swc/core'
  - esbuild
  - unrs-resolver
